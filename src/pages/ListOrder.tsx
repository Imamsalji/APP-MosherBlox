import { useEffect, useState } from "react";
import CyberpunkSpinner from "../component/transaksi/CyberpunkSpinner";

export default function ListOrder() {
    return <CyberpunkSpinner size={80} text="Loading" />;
}